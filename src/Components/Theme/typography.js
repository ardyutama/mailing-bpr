import palette from "./palette";

const FONT_PRIMARY = 'Inter';

const grey = palette.grey[800];

const typography = {
    fontFamily: FONT_PRIMARY,
    allVariants: {
        color: palette.common.black,
    },
};
 
export default typography;